## This is a mock webapp, intended to learn how to use a stack. It's not functional.

Simple post webapp to learn Firebase, Next.js and a bit of SEO.

### Usage:

`npm i && npm run dev`
and navigate to `http://localhost:3000`
